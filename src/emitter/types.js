const POSTFIX = `DN`

export const ACTION = `${POSTFIX}@ACTION`
export const ACTION_RECEIVE = `${ACTION}@RECEIVE`

export const STORE = `${POSTFIX}@STORE`
export const STORE_RECEIVE = `${STORE}@RECEIVE`

export const SUBMIT = `${POSTFIX}@SUBMIT`
export const SUBMIT_RECEIVE = `${SUBMIT}@RECEIVE`

export const EXIT = `${POSTFIX}@EXIT`
export const EXIT_RECEIVE = `${EXIT}@RECEIVE`

export const ACCEPT = `${POSTFIX}@ACCEPT`
export const ACCEPT_RECEIVE = `${ACCEPT}@RECEIVE`

export const PENDING = `${POSTFIX}@PENDING`
export const PENDING_RECEIVE = `${PENDING}@RECEIVE`

export const REJECT = `${POSTFIX}@REJECT`
export const REJECT_RECEIVE = `${REJECT}@RECEIVE`

export const SUBMIT_BY_REVIEWER = `${POSTFIX}@SUBMIT_BY_REVIEWER`
export const SUBMIT_BY_REVIEWER_RECEIVE = `${SUBMIT_BY_REVIEWER}@RECEIVE`
